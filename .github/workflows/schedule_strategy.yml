name: Scheduled Strategy

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

run-name: Scheduled Strategy

jobs:
  play:
    continue-on-error: true
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Check schedule toggle
      id: guard
      run: |
        if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
          echo "enabled=true" >> "$GITHUB_OUTPUT"
          exit 0
        fi
        if [ "${{ vars.ENABLE_SCHEDULE }}" = "true" ]; then
          echo "enabled=true" >> "$GITHUB_OUTPUT"
        else
          echo "::notice::Schedule disabled. Set ENABLE_SCHEDULE repo variable to \"true\" to enable."
          echo "enabled=false" >> "$GITHUB_OUTPUT"
        fi

    - name: Install dependencies
      if: steps.guard.outputs.enabled == 'true'
      run: pip install requests

    - name: Run strategy
      if: steps.guard.outputs.enabled == 'true'
      env:
        GITHUB_TOKEN: ${{ secrets.GAME_TOKEN }}
        SERVER_URL: ${{ secrets.SERVER_URL }}
      run: python strategy.py

